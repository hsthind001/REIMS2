# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

dev-debug.log
# Dependency directories
node_modules/
# Environment variables
.env
.vscode
# OS specific

# Backups (generated by backup scripts)
backups/ 

# Cursor MCP configuration (contains API keys)
.cursor/mcp.json 

# Task files
# tasks.json
# tasks/ 

# Local environment files
.env.local
.git-credentials
.venv/

# Test result files (regenerable)
*_TEST_RESULTS.json
TEST_RESULTS.json
FRONTEND_TEST_RESULTS.json
API_TEST_RESULTS.json
COMPLETE_VALIDATION_RESULTS.json
EXTRACTION_VALIDATION_RESULTS.json
final_quality_statistics.json

# Generated files
regenerated_*.pdf
*_data_quality_report_*.html

# Docker context
.dockerignore

# ============================================================================
# REIMS2 DUPLICATE PREVENTION & CLEANUP
# ============================================================================

# Backup files (NEVER commit these)
*.backup
*.backup.*
*.old
*.old.*
*_OLD
*_OLD.*
*_BACKUP
*_BACKUP.*

# Temporary files
*.tmp
*.temp
*~
.*.swp
.*.swo

# Documentation archives (keep in docs/archive/ only)
*_ARCHIVED_*
*_DEPRECATED_*

# Cleanup reports (regenerable)
CLEANUP_REPORT_*.md
CLEANUP_LOG_*.md

# Large generated documentation (should be split/archived if >5MB)
# Add specific files here if they become too large

# Coverage reports (HTML)
htmlcov/
.coverage
coverage.xml

# Python cache
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Distribution / packaging
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Docker optimization artifacts
Dockerfile.*.backup
docker-compose.*.backup
*-entrypoint.sh.backup

# Node modules are already ignored above
# But being explicit about large dependencies
node_modules/
.pnp
.pnp.js

# Build artifacts
dist/
build/
*.bundle.js
*.bundle.css

# IDE workspace files
.vscode/settings.json
.vscode/launch.json
.idea/workspace.xml
.idea/tasks.xml
